import 'animal.dart';
import 'singleton.dart';

void main() {
  Singleton.getIntance();
 
}

enum EMPLOYEE {
  MANAGER,
  Teacher,
  Student,
}

/// (1) Anynomnous Constractor >> Animal()
/// (2) static >> variable , method
/// (3) Encapsulation
/// (4) Singleton (only one object)
/// (5) Inheritance ( extends ) ... single inheritance .. multiInhertiance( Mixins)
/// (6) Polymorphism ( Ovveride , overLoading... java)..
/// (7) Abstraction ( abstract , Interface)
/// (8) Mixins , Enum, Extensions


